(function () {
  window.ACTINUANCE_SANITY_CONFIG = {
    projectId: "YOUR_SANITY_PROJECT_ID",
    dataset: "production",
    apiVersion: "2024-10-01",
    useCdn: true,
    // Optional: increase if needed on list pages.
    maxItems: 24
  };
})();
